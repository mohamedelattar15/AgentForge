apiVersion: v1
kind: Service
metadata:
  name: agentforge
  labels:
    app: agentforge
spec:
  selector:
    app: agentforge
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
